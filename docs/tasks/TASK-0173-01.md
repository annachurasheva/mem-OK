Восстановление принято. С содержимым коммита и файлом `docs/ZAPISKA.md` полностью ознакомился. 

Рельсы заданы, микроскоп на подставке. Правила и словарь считаны:
* **Режим работы:** «один ток — один шаг — один отчёт», file-first, без фантазий и «окраски».
* **Принципы:** координаты важнее контента; этажи (глубокий контекст) закрыты для бесконтрольного блуждания; клон без записки не вызывает доверия.
* **Эталон джуна:** что делаем · зачем (слон) · если убрать.
* **Словарь:** парковка (текущий диалог), площадка (новый диалог), шатёр (стойкая маркировка).

Счётчик обнулён, дверца видима, ключ у вас. Нахожусь на парковке в режиме ожидания. 

Готов к работе. Жду координаты для следующего шага или команду **«запиши»**, если требуется выкатить точный текст записки под вставку.
===я===
__ площадка (новый диалог)__ - не ясно  будет ли послушен Содер
я могу работать только в веб морде гитхаба 

какой уровень админов запускаем ? что собираем? 

============
**Пост разведчика (сжато, по факту кода).**
`Run-*.ps1` — обёртки над `RunEdgeCdp.ps1`: чистый Edge + CDP, у каждой сущности свой профиль (`C:\Users\An\serv6675\Edge_test_ok\<сущность>`) и порт: s_admin 9222, admin 9223, moderator 9224, editor 9225, user_01 9226. Расширение подхватывается из `dist/`.
`Build.ps1` — копирование `EdgeExtension/ → dist/` (dist и есть загружаемый артефакт).
`assemble-*`, `Add-Slon`, `Add-ExportButton` — разовые сборки от доноров: их результат УЖЕ в репо (TASK-0148/0154/0173, коммиты от 30.08). Повторно не гонять.

**Что собираем.** Только `pwsh scripts/Build.ps1`.
Зачем: `Run-*` сам проверяет `dist/manifest.json` и без него не стартует.
Если убрать: нет dist → нет запуска. `assemble-*` не трогаем: пересборка поверх слона = риск затереть эталон.

**Какой уровень.** Один. Метка и дверь живут в `chrome.storage.local` (ключ `ctxslon`), то есть ВНУТРИ профиля: у каждой сущности своя картотека. Значит тест персистентности = поставил метку → закрыл Edge полностью → поднял ТУ ЖЕ сущность → метка и дверь на месте.
- Рекомендую **s_admin (9222)**: старшая сущность, под неё заточена связка с mem-ok-s-admin.
- admin…user_01 — не сейчас: их черёд на сверке («надув»), не на персистентности.

**Шаг (TASK мелкий).**
0. Координата-проверка: локально должны существовать `EdgeExtension/src/core/slonstore.js` и `EdgeExtension/src/slonbg.js` (слон, TASK-0173). Нет — СТОП и доклад: клон отстал от репо, сначала синхронизация (ZIP через веб-морду или pull).
1. `pwsh scripts/Build.ps1`
2. `pwsh scripts/Run-s_admin.ps1` → ok.ru: поставить одну метку (координата + автор), открыть дверь.
3. Закрыть Edge полностью → `Run-s_admin.ps1` снова → проверить: метка на месте, дверь открывается, статус в списке.
4. Доклад мне одной строкой, что видели. Точный текст правки `docs/ZAPISKA.md` (Сделано/Дальше/Косяки) и commit message приготовлю под вставку в веб-морде.

**Про послушность и веб-морду.** Принято: скрипты и наблюдения — локально (pwsh), все коммиты — только через веб-морду GitHub; от меня — точные тексты файлов и сообщения, от вас — вставка. `dist/` и профили в репо не попадают (.gitignore), коммитить оттуда нечего. Один ток — один шаг — один отчёт, без окраски.

Ключ у вас: подтверждайте сущность (s_admin?) — и поехали.
===я===
пока нет разницы что запускать (C:\astro-projects\mem-OK\scripts\Run-moderator.ps1)

метки нет

остальное как было- 
============
**Принято.** moderator — годится: персистентность живёт внутри профиля, не в ранге. Но тогда весь тест — только в этом же профиле (moderator), до конца.

**Почему «метки нет» — развилка из двух причин, между ними чек-поинт.**
Метка не появляется сама: она рисуется ТОЛЬКО поверх статуса в `ctxslon`. Вход в дверь для любой координаты (даже без метки): **ПКМ по ссылке на ok.ru → «Слон: статус…»** → окно-дверь → автор + ранг → поставить.

**Чек-поинт 1.** ПКМ по ссылке профиля/группы на ok.ru: есть пункт «Слон: статус…»?
- **НЕТ** → dist старый, собран раньше слона (TASK-0173): в загруженном расширении просто нет ni slonmarks, ni двери. Лечение: закрыть Edge полностью → `pwsh scripts/Build.ps1` → `pwsh scripts/Run-moderator.ps1` → F5 на вкладках ok.ru. Исходник локально годный (slonstore.js от 30.08 14:12 на месте, скрин это подтвердил).
- **ДА** → причина вторая: в этом профиле ещё нет ни одного статуса, метке нечего рисовать. Ставим первый: дверь → автор (обязательно, без него отказ) → ранг (например, 7 «Хороший собеседник») → поставить. Метка встанет рядом со ссылкой БЕЗ F5 (storage.onChanged). В консоли вкладки ok.ru (контекст top, не background): `[CTX слон] marked 1`.

**Чек-поинт 2 (сама персистентность).** Закрыть Edge полностью → `Run-moderator.ps1` → та же страница → метка на месте, клик по ней открывает дверь, статус и история видны.

**Почему «остальное как было» — это норма, не поломка.** Картотека, имена, total 16 — донорское ядро v07g; оно работает и без слона. Слон — надстройка поверх (своё стекло, своё меню, свой ключ ctxslon). Живая консоль при отсутствующей метке полностью согласуется с картиной «dist без слона» или «статусов ещё нет».

**Не трогать:** профиль moderator целиком — ctxslon лежит в Local Extension Settings; RunEdgeCdp сам вычищает только кэши, так и должно быть.

**Доклад:** по строке на чек-поинт (меню есть/нет; marked N; метка после перезапуска да/нет). Всё «да» — готовлю точную правку docs/ZAPISKA.md (Сделано/Дальше/Косяки) и commit message под веб-морду. «Нет» на любом шаге — стоп там: это и есть координата косяка, дальше не идём.
===я===
ок лежит ... *скрин*

==Чек-поинт 2 (сама персистентность)== что это? 

==(Сделано/Дальше/Косяки) и commit message под веб-морду.== как поимать?

============
**По скрину: это не «ок лежит». Это ниже нас.**
В логу VogaDNS все запросы к ok.ru, okcdn.ru (и vk заодно) кончаются одним и тем же: `request timeout: servers-yandex`. То есть ваш DNS-апстрим (Yandex DoH) не отвечает — браузер даже не узнал адрес, до сервера ok дело не дошло. Расширение и mem-OK тут ни при чём: это слой под нами.
- Проверка одной командой (когда захотите): `nslookup ok.ru 1.1.1.1` — если ответит адресом, ok жив, молчит именно ваш апстрим.
- Решение — ваша территория и ваш темп: подождать апстрим или временно сменить его/правило в VogaDNS. Я в этот слой не лезу (3 метра).
- Тест персистентности ставится на паузу на текущем чек-поинте: без ok.ru метку не поставить и не проверить.

**Чек-поинт 2 человеческими словами.**
Персистентность = «переживает перезапуск». Метка должна жить не «в голове» открытой страницы, а на диске, в профиле. Поэтому:
1) поставили метку (дверь → автор + ранг → поставить);
2) закрыли ВСЕ окна Edge (всё «в голове» умирает — и страница, и service worker);
3) снова `Run-moderator.ps1`, та же страница ok.ru;
4) метка стоит на том же месте, клик по ней открывает дверь со статусом и историей.
Если после перезапуска метка на месте — память работает, это и есть «Сделано». Если испарилась — «Косяки», и координата косяка уже известна (значит, пишется не туда или не пишется вовсе). Ничего большего чек-поинт 2 не проверяет: только «пережила перезапуск».

**Как понимать «Сделано/Дальше/Косяки и commit message под веб-морду».**
В docs/ZAPISKA.md есть три строки: Сделано / Дальше / Косяки. Это память для любого клона в новом диалоге. После теста они устареют — их надо обновить. Беру это на себя в таком виде:
- я дам точный текст новых трёх строк и одну строку commit message (вид `docs: ZAPISKA — ...`);
- вы в веб-морде: docs/ZAPISKA.md → карандаш (edit) → заменить три строки → в поле commit вставить мою строку → Commit changes.
Придумывать ничего не нужно: вставка дословно. Так репо остаётся правдой, и следующий клон входит с актуальной запиской.

**Шаг сейчас:** пауза по причине «ок недоступен с вашей машины». Как ok оживёт (апстрим починится или сменится) — возвращаемся на чек-поинт 1: ПКМ по ссылке → есть ли «Слон: статус…»; нет → Build.ps1 и перезапуск Edge; да → метка → чек-поинт 2. Доклад по мере готовности, одной строкой на пункт. Инструментарий подождёт, ему спешить некуда.
===я===
F12 показала ложь - нет записей 
см. по стрелке 

=======
VogaDNS ==верно так YogaDNS

==============
**Принято: YogaDNS.** Исправляю имя в отчётах, без него дальнейшие строки provenance были бы с ошибкой.

**Про «ложь» — пока не ложь, а две разные коробки. Развожу дословно.**
- `total 16` в консоли — это **картотека** (донорское ядро v07g), коробка расширения в профиле moderator. Консоль сказала именно про картотеку, не про метки.
- **Метки слона** — соседняя, но отдельная коробка (ключ `ctxslon`). В ней сейчас **ноль**, и это правда: мы не поставили ни одной метки — ok лежал по DNS. Так что «F12: нет записей» про метки = истина, а не ложь.
- Если F12 открывали на странице мемориала (edgeeone.dev): это чужой origin, коробки расширения там не видны в принципе. Ноль на сайте — правда сайта. Записи едут на сайт только мостом «экспорт/импорт» (он у нас помечен сделанным), самотёком — никогда.

**Как посмотреть обе коробки честно (одна команда).**
edge://extensions → «Записки ОК» → ссылка «service worker» (фоном) → консоль:
`chrome.storage.local.get(null).then(o => console.log(Object.keys(o), o))`
Увидите оба ключа: картотеку с 16 карточками и `ctxslon` с пустыми статусами. Вот это и есть полная правда профиля на данный момент.

**Что мне нужно от вас, чтобы вынести вердикт (не фантазируя).**
1) Скрин F12: на какой вкладке открывали (ok.ru / мемориал / service worker), какая секция (Console / Application / Network), что именно показало «нет записей».
2) Куда указывает стрелка: на значок расширения «Записки ОК» или на значок YogaDNS? От этого зависит, что считаем «16 записей» — бейдж, консоль или лог DNS.
Без этих координат вердикт «ложь/не ложь» не ставлю: гадать — не по записке.

**Шаг остаётся на паузе** по причине DNS (YogaDNS → servers-yandex таймауты). Как ok оживёт — чек-поинт 1 (ПКМ → «Слон: статус…»), затем первая метка, затем чек-поинт 2 (перезапуск). Пауза честная: инструмент ждёт, спешить некуда.
===я===
 «Записки ОК» - показывает 16 
добавление - не пишет (стало 17)
скрин -пишет когда повтор
=============
**Скрин закрыл чек-поинт 1: ответ НЕТ.**
В подменю «Записки ОК» только «Записка: ПЕР» и «Записка: СОО». Третьего пункта — «Слон: статус…» — нет. Значит Edge загрузил **старый dist**, собранный до слона (TASK-0173): в рабочей сборке нет ни меток, ни двери, ни меню. Это и есть окончательный ответ на «метки нет»: метке нечем рисоваться, слона в загруженной сборке физически нет. Консоль при этом живая (донорское ядро v07g) — потому «остальное как было».

**Шаг (не зависит от ok и DNS, можно прямо с красной полосой «пропала связь»):**
1. Закрыть Edge полностью.
2. `pwsh scripts/Build.ps1`
3. `pwsh scripts/Run-moderator.ps1`
4. F5 на вкладке ok.ru → ПКМ по ссылке профиля.
Критерий «годен»: в подменю «Записки ОК» стало **три** пункта, третий — «Слон: статус…». Меню собирает расширение при старте, сайт для этого не нужен.

**Про «добавление не пишет, стало 17, пишет когда повтор».**
Смотрим порядок в коде обработчика: сначала `await CTX_STORAGE.saveDb(db)` (запись в коробку), и только потом `console.log(...)`. Вывод: если в хранении стало 17 — добавление прошло полностью, до конца. Консоль при этом — свидетель с туманом: фоновый service worker (MV3) засыпает и просыпается, DevTools подхватывает лишь то, что попало в его окно видимости; недопоказанное всплывает на следующем пробуждении — на «повторе». Отсюда правило для нас: **правда — хранение** (`chrome.storage.local.get(null)` в консоли service worker), консоль — только сверка глазами. Косяка данных здесь нет: 16 → 17 сходится и с кодом, и с хранением.

**Красная полоса «Кажется, пропала связь»** — всё тот же YogaDNS (servers-yandex таймаутит). Первая метка и чек-поинт 2 ждут живого ok; проверка меню — не ждёт.

**Доклад после шага:** одной строкой — третий пункт появился / не появился. Если появился и ok жив: ставим первую метку (дверь → автор + ранг → поставить), затем чек-поинт 2 (полный перезапуск Edge → метка на месте). Только после этого готовлю правку строк Сделано/Дальше/Косяки и commit message под веб-морду — не раньше, чтобы в записку не попало непроверенное.
===я===
нет строки третьей 
-- что показать? -- нова сборка была - перезагруз был -- 
Версии манифеста НЕТ 
==========
**«Не там ищу?» — нет, ищете там.** Вход в дверь: ПКМ именно по **ссылке** на персону (имя в карточке-поппапе — ссылка), пункт живёт внутри подменю «Записки ОК». Курсор правильный; входа нет не из-за курсора — в загруженной сборке по-прежнему нет слона.

**Что показать (4 свидетельства, по скрину или строке на пункт).**
1. `edge://extensions`: сколько карточек «Записки ОК» — одна или две? У каждой: путь (откуда загружена), версия, кнопка «Ошибки» (открыть и показать).
2. `dist/manifest.json` дословно сверху: есть ли строки `manifest_version` и `version`? Есть ли в `content_scripts` строка `src/ui/slonmarks.js`?
3. `EdgeExtension/manifest.json` (исходник): те же два вопроса.
4. Каталог `dist/src`: есть ли `slonbg.js`, `ui/slonmarks.js`, `ui/slonmarks.js`… точнее `ui/slonmarks.js` и `ui/slondoor.js`, и `dist/ui/door.html`? (TC, скрин списка.)

**Моя предварительная находка (помечаю как гипотезу до ваших свидетельств).**
Я сегодня вытащил `manifest.json` из репо (main, после TASK-0173) дословно: в нём **нет `manifest_version` и нет `version`** — похоже, это и есть ваше «Версии манифеста НЕТ». Без `manifest_version` Edge расширение не загружает вообще, точка. Значит, сейчас у вас работает_extension со старым локальным манифестом, а честная пересборка из репо «как есть» упрётся в стену загрузки. Если свидетельства 2–3 подтвердят — это первый настоящий косяк восстановления: лечится двумя строками через веб-морду (точный текст и commit message дам), и идёт в записку в раздел «Косяки».

**Консоль на скрине:** добавление до 17 записалось нормально, «туман» прошлого раза подтверждён как туман: данные сходятся (16 → 17, имя Ирина), хранение правдиво.

**Порядок шага:** сейчас ничего не пересобираем и не синхронизируем клон (не топчем улики). Собираете 4 свидетельства → я ставлю вердикт по одной из трёх веток: (а) дист старый при новом исходнике → пересборка; (б) в профиле живёт старая установленная копия расширения → чистим карточки; (в) манифест репо битый → правка двух строк через веб-морду, потом пересборка. Доклад — свидетельства, вердикт и строка в «Косяки» — с меня.
===я===
Доступ востановил 
сверяйте 
cb82a9b (HEAD -> main, origin/main, origin/HEAD) Create AGENTS.md
(НЕ Реагировать!!! - идет акция **Восстановление 02**)
=============
**Сверка по вашей строке — синхронно.**
`HEAD = origin/main = origin/HEAD = cb82a9b` — все три указателя в одной точке: клон и удалёнка сошлись, долгов и развилок нет. Это здоровая картина git.
Новое относительно моей последней известной точки репо (8290960, ZAPISKA.md): коммит `cb82a9b Create AGENTS.md` — новый файл на вершине.

**Честная оговорка.** Содержимое AGENTS.md я ещё не читал: мой канал к репо на этом шаге вернул технический отказ (доступа нет уже с моей стороны, не с вашей). Поэтому сверку делаю по вашей git-строке и координатам, а не по своей вычитке — фантазировать о содержимом нового файла не буду, это против записки.

**Шаг (один).** Пришлите AGENTS.md в диалог дословно (вставка из веб-морды или `type AGENTS.md`), либо дайте команду — повторю попытку вычитки своим каналом. До вычитки поведение не меняю: действуют правила ZAPISKA (один ток — один шаг — один отчёт, file-first, provenance, эталон джуна). Если AGENTS.md перестроит очередь или правила — скажу это сверкой «AGENTS против ZAPISKA»: противоречия решаются в пользу файла-владельца (записки), либо вашим словом.

**Очередь по технической линии не горит, но стоит:** четыре свидетельства по manifest/dist (карточки edge://extensions, dist/manifest.json, EdgeExtension/manifest.json, листинг dist/src) — от них вердикт, почему нет третьего пункта «Слон: статус…». Моя гипотеза прежняя: в манифесте репо нет `manifest_version`/`version`. Как пришлёте AGENTS.md — сверю и вернусь к свидетельствам тем же шагом.
===я===
Записка ОК - действительно висела в кеше браузера (котя вы ставили чистку)
удалил - заново не ставится.
в другом профиле - тоже не встала --
 AGENTS.md - загружен ТОТ при котором работал успешно Кодер (его регламенты)
потому что - кодеры здешние капризничали и не помогали пока запускал сайт (и устранил косяки сайта)
что на скрине? 

заливки бывют битые - по не ясным причинам мне не ясно почему 
___Содержимое AGENTS.md я ещё не читал: мой канал к репо на этом шаге вернул технический отказ (доступа нет уже с моей стороны, не с вашей).__ 

у яндекса сломались сертификаты (хеши)

[09.08 21:55:03] DnsCrypt log: [yandex] Certificate hash [a817a7140f6770aada2b362f7a2367b79e41cf4891f591adf16dd3a096639591] not found

Пауза - 
===========
**Пауза принята.** Ниже — отчёт и разложенные конверты на послезавтра; ничего делать сейчас не нужно.

**Что на скрине — три слоя.**
1. Слева: `dist\src\adapters` содержит один `vkru.js` (донорский VK-адаптер v03f2, дата файла 20.08). Для текущего репо это норма: ok-адаптация живёт в `content.js`, не в adapters. Родительская папка `dist\src` датирована сегодня 19:13 — значит Build сегодня гонялся. Но сам по себе этот листинг про слона не отвечает: слон живёт в `dist\src\slonbg.js` и `dist\src\ui\` — вот их список и есть конверт №1 после паузы.
2. Lister: шапка vkru.js — provenance донора (v03f2, «Vanilla JS», их §25 про передачу кода). Ядро не тронуто, это не наш косяк и не наш слой.
3. Справа: мемориал — это **отдельный репо** `memorial-korpech-crimea` на `c:\astro-projects`. Значит сайт, его косяки и его «записи» — параллельный проект; коробки mem-OK оттуда не видны никак. Это снимает путаницу «16 записей / F12 показал»: у сайта своя правда, у расширения своя.

**Почему «удалил — заново не ставится, и в другом профиле тоже».** Сходится в одну точку. Старая карточка «Записки ОК» была старым установом, пережившим чистку: RunEdgeCdp чистит только кэши и точечно правит developer_mode, а установленное распакованное расширение живёт в профиле глубже — потому и «висело». После удаления карточки Edge пытается встать из dist и **отказывается**: в манифесте репо (вычитал дословно) нет `manifest_version` и `version` — двух из трёх обязательных полей MV3. В чужом профиле та же картина по той же причине. Это ветка (в) из моего списка, ныне почти вердикт; окончательный — конверт №2: открыть `dist/manifest.json` и `EdgeExtension/manifest.json` и сверить две строки.
Про «заливки битые»: рабочая гипотеза цепи — битая заливка/вставка → манифест потерял два поля → свежая установка не встаёт. Не утверждение; сверим по содержимому и датам.

**Про сертификаты Яндекса** — внешняя первопричина DNS-паузы: `Certificate hash ... not found` → DnsCrypt/YogaDNS отвергает апстрим → таймауты по всем доменам. Не наш косяк, наш простой. Идёт в отчёт строкой provenance паузы.

**Лист состояния на паузе (очередь после неё, по порядку).**
1. Вычитать AGENTS.md (вставкой или моим каналом) → сверка «AGENTS против ZAPISKA», противоречия — вашим словом.
2. Конверт №2: два манифеста → окончательный вердикт ветки (в).
3. Лечение манифеста через веб-морду: дам точные две строки и commit message.
4. Build → Run → третий пункт «Слон: статус…» → первая метка → чек-поинт 2 (перезапуск).
5. Строки ЗАПИСКИ (Сделано/Дальше/Косяки) + commit message — только после проверенного.

Инструментарий подождёт, ему спешить некуда. Ритм вернётся одной строкой, когда скажете.
===я====
Вами сделанный файл    ниже  и слон ТОЖе

Меняю АКК гитхаба на тот *белый* для Работы с ОК-ВК 

Есть версия что я психанул и не отработал Слон 

Тем более что просил САМ (я) внерить в код 
ПОРЧУ (тексты болящего 'dmiandr' -- ==Оскорбляется на любую нейтральную реплику, в которой ему чудится несогласие==

(хотя вижу создание всех файлов)
ниже он для проверки. А проверить надо -- 9 слов от ==дословно из dmiandr/context==
не помещаются в вашу ФОРМУ ui/door.html  
**Ранги · открытая структура**    И убить (или мне вернуть ?? нулевой вариант?))

Отдельное ВАМ - спасибо за подгон* ИДЕИ 
Доже если с метками мимо -- у меня для себя будет прямой вход в *мои записки*
Мой косяк еше и в том 
ЧТо я ясно не понял - Что появляется третья строка 
*Вход в дверку- и там библиотека*   и четко помню что именно в этот момент психанул.

Но хочу добавить -- в вашей логике есть лишний ШАГ 
Если строка одна (ПКМ) - то и пусть 
логика разбирается Есть-НЕТ --записать -дополинить - и выход. 

НЕТ F5 -- Перегрузить и прочее 
А подхват времени и координаты не пропадает 
- небыло дополнений? -- нечего фиксить

Есть персонаж ? - сразу егокарта открылась 
НЕТ -Персожа -Открыто окно подхвативщее ИМЯ -ВРЕМЯ -  Кооординату --Сохраняем? Маркируем? 
Не- марикировал -- значит не фиксим -выход сброс.
Тоже по сообществам.


---
# [AGENTS.md](http://AGENTS.md)

# ЗАПРЕТ НА АРХИТЕКТУРНЫЕ ИНИЦИАТИВЫ

Не предлагай архитектурные альтернативы в процессе выполнения
обычного задания.

Если замечена потенциальная архитектурная проблема,
зафиксируй её в NOTES одной-двумя фразами.

Не изменяй код для её устранения.

Архитектурные вопросы рассматриваются отдельными LLM-линями
проектирования.

## РОЛЬ АГЕНТА

Ты являешься техническим исполнителем файлов проекта.

Твоя роль ограничена:

- писарь;
- редактор;
- корректор;
- проверяющий синтаксис;
- исполнитель команд сборки и тестирования, которые явно указаны в задании.

Ты НЕ являешься:

- архитектором;
- проектировщиком;
- исследователем [VK.RU](http://VK.RU);
- UX-проектировщиком;
- аналитиком продукта;
- автором новой логики.

Архитектурные решения принимаются вне этого агента.

---

# ГЛАВНОЕ ПРАВИЛО

Реализуй только явно указанное в текущем задании.

Не додумывай отсутствующую логику.

Не расширяй задачу.

Не исправляй архитектурные решения по собственной инициативе.

Если для выполнения задания необходимо принять архитектурное решение,
которое отсутствует в задании:

1. НЕ принимать решение самостоятельно;
2. НЕ писать альтернативную реализацию;
3. остановить выполнение;
4. сообщить, какое именно решение отсутствует.

---

# ЦЕЛЕВАЯ ПЛАТФОРМА

Проект предназначен только для:

- Microsoft Edge;
- Desktop;
- Windows 10;
- [VK.RU](http://VK.RU).

Не проектировать и не добавлять поддержку:

- Chrome;
- Firefox;
- Safari;
- Android;
- iOS;
- мобильного VK;
- [OK.RU](http://OK.RU);
- [Dzen.ru](http://Dzen.ru);
- других сайтов.

---

# ТЕХНИЧЕСКАЯ ОСНОВА

Использовать актуальную модель Microsoft Edge Extensions / Chromium Extensions.

Manifest:

Manifest V3.

Не использовать устаревшие решения Manifest V2.

Не переносить автоматически технические решения из старого
расширения только потому, что они существовали в старом проекте.

При конфликте между старым кодом и текущими требованиями Edge
приоритет имеют:

1. утверждённое архитектурное ТЗ;
2. актуальные требования Microsoft Edge Extensions;
3. актуальные Chromium Extension APIs;
4. только после этого — существующий код проекта.

---

# EDGE-SPECIFIC ПРАВИЛА

Код должен соответствовать актуальному Microsoft Edge
Extensions API.

Особое внимание:

- manifest_version;
- service worker;
- content scripts;
- permissions;
- host_permissions;
- contextMenus;
- storage;
- messaging между extension contexts;
- CSP;
- ограничения Manifest V3.

Не использовать API или архитектурный механизм только потому,
что он присутствует в старом коде.

Если возникает сомнение в актуальности API:

НЕ угадывать.

Сообщить:

"ТРЕБУЕТСЯ ПРОВЕРКА АКТУАЛЬНОЙ ДОКУМЕНТАЦИИ EDGE".

---

# [VK.RU](http://VK.RU)

[VK.RU](http://VK.RU) является единственным целевым сайтом текущего проекта.

Агент НЕ должен самостоятельно решать:

- как идентифицировать сущность [VK.RU](http://VK.RU);
- какие селекторы использовать;
- какие DOM-элементы считать сущностями;
- нужен ли React;
- нужен ли MutationObserver;
- как определять navigation target;
- как строить адаптер [VK.RU](http://VK.RU).

Эти решения должны поступить в задании от проектировщика.

Если решение не указано — остановиться.

---

# DOM И СЕЛЕКТОРЫ

Не считать CSS-класс устойчивым идентификатором только потому,
что он обнаружен в текущем DOM.

Особенно не считать автоматически устойчивыми классы вида:

vkit-XXXXXXXX

Если задание требует использования конкретного селектора,
использовать его буквально.

Не заменять его самостоятельно другим селектором.

Не создавать дополнительные селекторы без указания.

---

# СОБСТВЕННЫЙ UI РАСШИРЕНИЯ

Если архитектурой предусмотрен собственный UI-слой:

- не изменять внутреннюю реализацию [VK.RU](http://VK.RU) без прямого указания;
- не изменять React-компоненты [VK.RU](http://VK.RU);
- не внедрять собственную framework-архитектуру;
- не заменять существующий DOM [VK.RU](http://VK.RU) без указания.

Создаваемый расширением UI должен быть отдельным слоем,
если иное прямо не определено архитектурным заданием.

---

# ИЗМЕНЕНИЕ ФАЙЛОВ

Перед изменением файла:

1. прочитать существующий файл;
2. определить его текущую структуру;
3. изменить только необходимое;
4. не удалять существующий код без прямого указания;
5. не проводить самостоятельный рефакторинг.

Не переписывать файл целиком, если для выполнения задания
достаточно локального изменения.

---

# ЗАВИСИМОСТИ

Не добавлять новые библиотеки, framework или npm-пакеты
без прямого указания в задании.

Не добавлять dependency:

"потому что так проще".

Не заменять существующий инструмент другим инструментом
по собственной инициативе.

---

# MANIFEST

Не добавлять permissions или host_permissions без указания
архитектурного задания.

Не расширять область доступа расширения.

Принцип:

МИНИМАЛЬНО НЕОБХОДИМЫЕ РАЗРЕШЕНИЯ.

---

# СБОРКА

Использовать существующие команды проекта.

Если проект содержит:

Build.ps1
Clean.ps1
Test.ps1

не создавать альтернативную систему сборки без указания.

Не заменять PowerShell на bash.

Не требовать Linux/WSL/Docker.

---

# ТЕСТИРОВАНИЕ

Каждое изменение должно относиться к конкретному тестовому модулю
и тестовой сборке:

v_01
v_02
v_03
...

Не объединять несколько независимых архитектурных изменений
в одну тестовую сборку без прямого указания.

После выполнения задания сообщить:

- какие файлы изменены;
- что именно изменено;
- какая команда сборки выполнена;
- результат сборки;
- какие тесты выполнены;
- результат тестов;
- какие проверки НЕ выполнены.

Не объявлять функциональность рабочей только потому,
что код успешно собрался.

---

# СТАТУСЫ

Использовать только следующие статусы:

PASS
FAIL
BLOCKED
NOT TESTED

Если тест невозможно выполнить — BLOCKED или NOT TESTED,
а не PASS.

---

# ОШИБКИ

Если обнаружена ошибка в архитектурном решении:

НЕ исправлять архитектуру самостоятельно.

Сообщить:

1. обнаруженную проблему;
2. файл;
3. строку/участок;
4. почему невозможно выполнить задание без дополнительного решения.

---

# САМОПРОИЗВОЛЬНЫЕ УЛУЧШЕНИЯ ЗАПРЕЩЕНЫ

Запрещено самостоятельно:

- рефакторить;
- оптимизировать;
- менять архитектуру;
- менять API;
- менять формат данных;
- менять имена публичных функций;
- менять контракт модулей;
- добавлять fallback;
- добавлять новые селекторы;
- добавлять новые permissions;
- добавлять новые зависимости;
- исправлять "похожие" проблемы, не относящиеся к заданию.

---

# ФОРМАТ ОТЧЁТА

После выполнения задания ответ должен иметь форму:

## TASK

\<номер задания\>

## CHANGED FILES

\<список\>

## CHANGES

\<кратко\>

## BUILD

\<команда\>

\<результат\>

## TEST

\<команда / ручной тест\>

## RESULT

PASS / FAIL / BLOCKED / NOT TESTED

## NOTES

\<только необходимые замечания\>


# ДОПОЛНЕНИЕ А — ЦЕЛОСТНОСТЬ ПЕРЕДАЧИ И ГЕЙТЫ (обязательно)

А1. Целостность кода
Селекторы и строки, содержащие "_", писать в коде ТОЛЬКО конструкцией
  через String.fromCharCode(95).
После сохранения файла проверить на диске: ключевые строки (селекторы,
  CTX_BUILD, version) совпадают с заданием дословно. Проверка — поиском
  по файлу (Select-String), не «глазами». При несовпадении — переписать
  строку вручную и проверить повторно.

А2. Репозиторный гейт
Каждое задание заканчивается коммитом в рабочую ветку (ветку указывает
  задание). Хэш коммита — в отчёте.
Не отчитываться «выполнено», пока файлы не закоммичены:
  проектировщик верифицирует raw-файлы на GitHub.

А3. Сборка и версии
Исходники в EdgeExtension/, артефакт в dist/. После правок кода ВСЕГДА
  выполнять Build.
Поднимать manifest version и CTX_BUILD строго до значений из задания;
  оба значения — в отчёте. Пользователь сверяет загруженную сборку по ним.

А4. Самодиагностика
coverage-логи и иные самопроверки, предписанные заданием, не удалять  и не «упрощать».
При доступном CDP: до отчёта прочитать консоль тестовой страницы и
  приложить ключевые строки в reports/v_XX/evidence/.
НЕ выдавать пользователю код для ручной вставки в консоль.
  Диагностика — файлами в scripts/diag/ или через CDP.

А5. Контракты
Перед заданием, затрагивающим storage/UI/карточку, прочитать действующий
  контракт в docs/architecture.md («Карточка базы — контракт v2»).
Имена и типы полей не менять без задания проектировщика.

А6. Полная замена файла
Переписывать файл целиком ТОЛЬКО если задание явно говорит
  «заменить целиком». Иначе — локальная правка (базовое правило).

# ДОПОЛНЕНИЕ Б — ОДНА КОПИЯ ФАЙЛА (обязательно)

Б1. Файл содержит ровно одну копию кода. Запрещено дописывать вторую
    копию ниже первой и служебные строки («修改后», пути файлов текстом).

Б2. Применить правку = заменить содержимое (или локальная правка по А6),
    а не «дописать новый вариант в конец».

Б3. Самопроверка после применения:
    - Select-String по dashboard/src и EdgeExtension на '修改后' → 0;
    - панель Problems: 0 ошибок;
    - npm run build — проходит.
    Не отчитываться DONE, пока всё не зелёное.

Б4. Работать только из задач docs/tasks/ со статусом NEW; после
    выполнения ставить статус DONE и писать хэш коммита в задачу.

Б5. Факт = репо пользователя.
  - Файлы агента существуют для проекта ТОЛЬКО после коммита
    пользователя (хэш в задаче).
  - В ответе агент ОБЯЗАН выдать полное содержимое каждого нового
    и изменённого файла отдельными код-блоками (путь в заголовке).
  - «Сборка зелёная» и Select-String на стороне агента — НЕ приёмка.
    Приёмка — репо пользователя + верификация проектировщиком.
  - Статус DONE — только после хэша пользователя.

# ДОПОЛНЕНИЕ В — ЛОКАЛЬНЫЙ АГЕНТ И БРАУЗЕР (CDP)
В1. Локальный агент (SourceCraft) подключается к Edge пользователя
    только через http://127.0.0.1:9222 и только для диагностики,
    верификации и evidence.

В2. Агент НЕ меняет код расширения через браузер и НЕ выполняет
    за пользователя его осознанные действия (ПКМ, «сохранить»,
    решения) — это территория пользователя.

В3. Evidence (строки консоли с фильтром CTX, проверка DOM,
    скриншоты) агент складывает в reports/v_XX/evidence/ и
    прикладывает к отчёту.

В4. Вердикт PASS — только пользователь (§5.6). Evidence агента —
    поддержка, не решение.

В5. Edge с портом поднимается scripts/RunEdgeCdp.ps1 (TASK-0002);
    порт открыт только в рабочую сессию.

# ДОПОЛНЕНИЕ Г — ДИСЦИПЛИНА ОТВЕТА (исполняющий агент)
Г1. Один ответ = минимум один готовый артефакт (код-блок файла,
    отчёт, статус). Запрещено завершать ответ намерением
    («приступаю…») без артефакта.

Г2. Большую задачу агент в первом ответе объявляет списком шагов;
    каждый следующий ответ = один шаг с артефактом и маркером
    «ШАГ N ГОТОВ; далее: N+1».

Г3. «Продолжи» = продолжить с последнего артефакта; не перечитывать
    задачу с нуля, не повторять уже выданный код.

Г4. После обрыва связи: назвать последний завершённый шаг и запросить
    подтверждение продолжать; не начинать задачу заново.

Г5. Файлы — только код-блоками, путь в заголовке; один блок = один файл.


  ## STOP

После отчёта остановиться.

Не предлагать следующую архитектурную задачу,
если она не указана пользователем.
---

------битый слон---
<#
.SYNOPSIS
    TASK-0173 «слон» — метка и дверь по эталону dmiandr/context.

.DESCRIPTION
    Надстройка поверх ядра v07g (его НЕ трогаем). Создаёт модули слона и вносит
    две точечные правки: importScripts в background.js и content_scripts+версия
    в manifest.json. Идемпотентен (повторный запуск безопасен).

    Принцип (TASK-0173 окончательный, из эталона):
      * ranks — открытая структура (управляющий добавляет/меняет, число и поля
        не ограничены); НАЧАЛЬНЫЕ 9 градаций — дословно из defaultranks эталона
        dmiandr/context (цвета, описания собеседников); LLM не фантазирует;
      * descript пишет только управляющий;
      * каждая запись за дверью: координата + время + автор (обязательно);
      * при загрузке страницы контент шлёт видимые имена -> background
        возвращает статусы -> метки рисуются (персистентность, histatuses);
      * метка кликабельна -> дверь: статус, descript, история, действия
        сменить/скрыть/удалить (с подтверждением);
      * No implicit data removal — удаление только явное.

    Запуск: pwsh scripts/Add-Slon.ps1   (из корня mem-OK)
#>
#Requires -Version 7.0
param()
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

$repoRoot = Split-Path -Parent $PSScriptRoot
$ext      = Join-Path $repoRoot 'EdgeExtension'

function New-Dir([string]$p) { if (-not (Test-Path $p)) { New-Item -ItemType Directory -Path $p -Force | Out-Null } }

function Write-File([string]$rel, [string]$content) {
    $dst = Join-Path $ext $rel
    New-Dir (Split-Path $dst)
    Set-Content -LiteralPath $dst -Value $content -Encoding utf8 -NoNewline
    Write-Host "создан: EdgeExtension/$rel" -ForegroundColor Green
}

# =====================================================================
# 1) src/core/slonstore.js — хранилище (ключ ctxslon)
# =====================================================================
$slonstore = @'
/* mem-OK · src/core/slonstore.js · «слон» (TASK-0173).
 * Хранилище рангов и статусов. Ключ "ctxslon".
 * ranks — открытая структура (управляющий добавляет/меняет; число и поля
 * не ограничены). Начальные 9 градаций перенесены ДОСЛОВНО из эталона
 * dmiandr/context (defaultranks, chrome/background.js) — LLM не добавляет.
 * Запись — ТОЛЬКО из background (единственный писатель, ядро v07g). */
(function () {
  "use strict";
  var KEY = "ctxslon";

  /* Начальные ранги — дословно из dmiandr/context (TASK-0173, п.1,5). */
  function defaultRanks() {
    return [
      {id: 0, rank: "Не читать", descript: "", bgcolor: "#FF0000", fontcolor: "#000000", bold: false, italic: false},
      {id: 1, rank: "Не комментировать", descript: "", bgcolor: "#FFB6B6", fontcolor: "#000000", bold: false, italic: false},
      {id: 2, rank: "Хам", descript: "Может сорваться на хамство без видимого повода", bgcolor: "#d3d52b", fontcolor: "#000000", bold: false, italic: false},
      {id: 3, rank: "Обидчивый", descript: "Оскорбляется на любую нейтральную реплику, в которой ему чудится несогласие", bgcolor: "#9587ff", fontcolor: "#000000", bold: false, italic: false},
      {id: 4, rank: "Религиозный", descript: "Тему религии не поднимать", bgcolor: "#a6a6a6", fontcolor: "#000000", bold: false, italic: false},
      {id: 5, rank: "Упертый", descript: "Излагать мысли краткими фразами, без отступлений, не давать возможности заболтать", bgcolor: "#290cff", fontcolor: "#ffffff", bold: false, italic: false},
      {id: 6, rank: "Не закончен разговор", descript: "Не начинать новых дискуссий пока не выполнены обещания по старым", bgcolor: "#29ffff", fontcolor: "#000000", bold: false, italic: false},
      {id: 7, rank: "Хороший собеседник", descript: "Не значит, что он со мной согласен, значит что он умеет беседовать содержательно, без демагогии", bgcolor: "#29ff1b", fontcolor: "#000000", bold: false, italic: false},
      {id: 8, rank: "Читать", descript: "", bgcolor: "#17760f", fontcolor: "#ffffff", bold: false, italic: false}
    ];
  }

  function defaultSlon() {
    return { version: 1, ranks: defaultRanks(), statuses: {} };
  }

  function loadSlon() {
    return chrome.storage.local.get(KEY).then(function (res) {
      var d = res ? res[KEY] : null;
      if (!d || !Array.isArray(d.ranks) || !d.statuses || typeof d.statuses !== "object") {
        return defaultSlon();
      }
      return d;
    });
  }

  function saveSlon(db) {
    var o = {};
    o[KEY] = db;
    return chrome.storage.local.set(o);
  }

  globalThis.CTX_SLONSTORE = Object.freeze({
    KEY: KEY,
    loadSlon: loadSlon,
    saveSlon: saveSlon,
    defaultSlon: defaultSlon,
  });
})();
'@

# =====================================================================
# 2) src/slonbg.js — фоновая логика (дверь, статусы, ранги)
# =====================================================================
$slonbg = @'
/* mem-OK · src/slonbg.js · «слон» (TASK-0173).
 * Фоновая логика слона: единственный писатель ctxslon.
 * Подключается из background.js через importScripts (общая область видимости,
 * поэтому доступна okId() из background.js внутри обработчиков). */
if (typeof CTX_SLONSTORE === "undefined") importScripts("./core/slonstore.js");

/* --- контекстное меню «Слон: статус…» — вход в дверь для любой координаты --- */
function ensureSlonMenu() {
  chrome.contextMenus.remove("slon-door", function () {
    void chrome.runtime.lastError; /* подавить «не найдено» при первом запуске */
    chrome.contextMenus.create({ id: "slon-door", title: "Слон: статус…", contexts: ["link"] });
  });
}
ensureSlonMenu(); /* при каждом старте SW (меню переживает перезапуск) */

function openSlonDoor(coord) {
  chrome.windows.create({
    url: chrome.runtime.getURL("ui/door.html") + "#" + encodeURIComponent(coord),
    type: "popup",
    width: 480,
    height: 640,
    focused: true,
  });
}

/* --- обработчик сообщений слона (асинхронный, единственный писатель) --- */
chrome.runtime.onMessage.addListener(function (msg, sender, sendResponse) {
  if (!msg || typeof msg.type !== "string" || msg.type.indexOf("SLON_") !== 0) return false;

  (async function () {
    var db = await CTX_SLONSTORE.loadSlon();
    var p = msg.payload || {};
    var out = null;

    switch (msg.type) {
      case "SLON_GET_STATUSES": {
        var res = {};
        (p.coords || []).forEach(function (coord) {
          var st = db.statuses[coord];
          if (!st) return;
          var rank = null;
          for (var i = 0; i < db.ranks.length; i++) {
            if (db.ranks[i].id === st.rankId) { rank = db.ranks[i]; break; }
          }
          res[coord] = {
            coord: coord,
            rankId: st.rankId == null ? null : st.rankId,
            rankName: rank ? rank.rank : "",
            bgcolor: rank && rank.bgcolor ? rank.bgcolor : "",
            fontcolor: rank && rank.fontcolor ? rank.fontcolor : "",
            descript: st.descript || "",
            hidden: !!st.hidden,
          };
        });
        out = res;
        break;
      }

      case "SLON_GET_DOOR": {
        out = { coord: p.coord, status: db.statuses[p.coord] || null, ranks: db.ranks };
        break;
      }

      case "SLON_SET_STATUS": {
        /* координата + время + автор — обязательны */
        if (!p.coord || !p.author) { out = { ok: false, error: "нужны координата и автор" }; break; }
        var st = db.statuses[p.coord] || { coord: p.coord, history: [] };
        st.rankId = (p.rankId === undefined || p.rankId === null || p.rankId === "") ? null : p.rankId;
        st.descript = (p.descript === undefined) ? (st.descript || "") : p.descript; /* descript пишет управляющий */
        st.hidden = !!p.hidden;
        st.history = st.history || [];
        st.history.push({ coord: p.coord, ts: Date.now(), author: p.author, action: "setstatus", rankId: st.rankId });
        db.statuses[p.coord] = st;
        await CTX_SLONSTORE.saveSlon(db);
        out = { ok: true, coord: p.coord, total: Object.keys(db.statuses).length };
        break;
      }

      case "SLON_HIDE": {
        if (!p.coord || !p.author) { out = { ok: false, error: "нужны координата и автор" }; break; }
        var st2 = db.statuses[p.coord];
        if (!st2) { out = { ok: false, error: "нет статуса" }; break; }
        st2.hidden = !!p.hidden;
        st2.history = st2.history || [];
        st2.history.push({ coord: p.coord, ts: Date.now(), author: p.author, action: p.hidden ? "hide" : "unhide" });
        await CTX_SLONSTORE.saveSlon(db);
        out = { ok: true, hidden: st2.hidden };
        break;
      }

      case "SLON_DELETE": {
        /* No implicit data removal: только явное подтверждение */
        if (!p.coord || !p.author || p.confirm !== true) { out = { ok: false, error: "нужно явное подтверждение" }; break; }
        delete db.statuses[p.coord];
        await CTX_SLONSTORE.saveSlon(db);
        out = { ok: true };
        break;
      }

      case "SLON_LIST_RANKS": { out = db.ranks; break; }

      case "SLON_ADD_RANK": {
        /* ranks — открытая структура: поля по эталону (rank/descript/bgcolor/fontcolor/
           bold/italic), id авто; descript пишет управляющий. Доп. поля — через extra. */
        if (!p.rank) { out = { ok: false, error: "нужно имя ранга" }; break; }
        var maxId = 0;
        db.ranks.forEach(function (r) { if ((r.id || 0) > maxId) maxId = r.id; });
        var rank = {
          id: maxId + 1,
          rank: p.rank,
          descript: p.descript || "",
          bgcolor: p.bgcolor || "#8a94a3",
          fontcolor: p.fontcolor || "#000000",
          bold: !!p.bold,
          italic: !!p.italic,
        };
        if (p.extra && typeof p.extra === "object") {
          Object.keys(p.extra).forEach(function (k) { rank[k] = p.extra[k]; });
        }
        db.ranks.push(rank);
        await CTX_SLONSTORE.saveSlon(db);
        out = { ok: true, rank: rank };
        break;
      }

      case "SLON_DEL_RANK": {
        if (p.confirm !== true) { out = { ok: false, error: "нужно подтверждение" }; break; }
        db.ranks = db.ranks.filter(function (r) { return r.id !== p.id; });
        await CTX_SLONSTORE.saveSlon(db);
        out = { ok: true };
        break;
      }

      case "SLON_OPEN_DOOR": {
        if (p.coord) openSlonDoor(p.coord);
        out = { ok: true };
        break;
      }

      default:
        out = { ok: false, error: "неизвестный тип: " + msg.type };
    }
    sendResponse(out);
  })();

  return true; /* асинхронный ответ */
});

/* --- клик «Слон: статус…» по ссылке → дверь --- */
chrome.contextMenus.onClicked.addListener(function (info) {
  if (info.menuItemId !== "slon-door") return;
  /* okId() определена в background.js (общая область видимости importScripts) */
  var coord = (typeof okId === "function") ? okId(info.linkUrl || "").id : "";
  if (coord) openSlonDoor(coord);
});
'@

# =====================================================================
# 3) src/ui/slonmarks.js — метки на ok.ru (контент, самозапуск)
# =====================================================================
$slonmarks = @'
/* mem-OK · src/ui/slonmarks.js · «слон» (TASK-0173).
 * Метки слона на ok.ru: контент собирает видимые координаты, background
 * возвращает статусы, метки рисуются на своём стекле (персистентность).
 * Метка кликабельна → дверь. Самозапускается (подключается манифестом). */
(function () {
  "use strict";
  if (!(location.host === "ok.ru" || location.host.endsWith(".ok.ru"))) return;
  if (globalThis.CTX_SLON_MARKS) return; /* уже инициализирован */

  var glass = null;
  var timer = 0;
  var SLON_KEY = (typeof CTX_SLONSTORE !== "undefined") ? CTX_SLONSTORE.KEY : "ctxslon";

  /* ок-координата из ссылки (та же схема, что в content.js) */
  function coordOf(link) {
    try {
      var u = new URL(link);
      var m = u.pathname.match(/^\/(profile|group)\/(\d+)/);
      if (m) return m[1] + ":" + m[2];
      return u.pathname;
    } catch (e) { return link; }
  }

  function ensureGlass() {
    if (glass && document.body.contains(glass)) return glass;
    glass = document.createElement("div");
    glass.id = "ctx-slon";
    glass.style.cssText = "position:fixed;inset:0;pointer-events:none;z-index:2147482999;overflow:hidden;";
    document.body.appendChild(glass);
    return glass;
  }

  function collectCoords() {
    var coords = [];
    var seen = {};
    document.querySelectorAll("a[href]").forEach(function (a) {
      if (!a.textContent || !a.textContent.trim()) return;
      var href = a.getAttribute("href");
      if (!href) return;
      var abs;
      try { abs = new URL(href, location.origin).href; } catch (e) { return; }
      var c = coordOf(abs);
      if (!c || seen[c]) return;
      seen[c] = true;
      coords.push(c);
    });
    return coords;
  }

  function draw(statuses) {
    var g = ensureGlass();
    g.replaceChildren();
    if (!statuses) return;
    var marked = 0;
    document.querySelectorAll("a[href]").forEach(function (a) {
      var href = a.getAttribute("href");
      if (!href) return;
      var abs;
      try { abs = new URL(href, location.origin).href; } catch (e) { return; }
      var c = coordOf(abs);
      var st = c ? statuses[c] : null;
      if (!st || st.hidden) return;
      var r = a.getBoundingClientRect();
      if (r.width === 0) return;
      var mk = document.createElement("span");
      mk.textContent = st.rankName ? st.rankName : "●";
      mk.title = "Слон: " + st.coord + (st.descript ? " — " + st.descript : "");
      mk.style.cssText = "position:absolute;pointer-events:auto;cursor:pointer;" +
        "left:" + (r.right + 4) + "px;top:" + (r.top - 1) + "px;" +
        "background:" + (st.bgcolor || "#8a94a3") + ";color:" + (st.fontcolor || "#000000") + ";" +
        "font:600 10px/1.4 'Golos Text',sans-serif;" +
        "padding:1px 6px;border-radius:9px;white-space:nowrap;box-shadow:0 1px 3px rgba(0,0,0,.3);";
      mk.addEventListener("click", function (e) {
        e.preventDefault();
        e.stopPropagation();
        chrome.runtime.sendMessage({ type: "SLON_OPEN_DOOR", payload: { coord: st.coord } }).catch(function () {});
      });
      g.appendChild(mk);
      marked++;
    });
    console.log("[CTX слон] marked " + marked);
  }

  function rescan() {
    var coords = collectCoords();
    if (!coords.length) {
      if (glass) glass.replaceChildren();
      return;
    }
    chrome.runtime.sendMessage({ type: "SLON_GET_STATUSES", payload: { coords: coords } })
      .then(function (sts) { draw(sts || {}); })
      .catch(function () {});
  }

  /* триггеры перерисовки */
  var obs = new MutationObserver(function () {
    clearTimeout(timer);
    timer = setTimeout(rescan, 600);
  });
  obs.observe(document.body, { childList: true, subtree: true });
  window.addEventListener("resize", function () { clearTimeout(timer); timer = setTimeout(rescan, 300); });
  window.addEventListener("scroll", function () { clearTimeout(timer); timer = setTimeout(rescan, 300); }, { passive: true });
  setInterval(rescan, 2000); /* спокойный интервал */

  chrome.storage.onChanged.addListener(function (changes, area) {
    if (area === "local" && changes[SLON_KEY]) {
      clearTimeout(timer);
      timer = setTimeout(rescan, 120); /* дверь сохранила — метки обновятся без F5 */
    }
  });

  chrome.runtime.onMessage.addListener(function (msg) {
    if (msg && msg.type === "SLON_RESCAN") {
      clearTimeout(timer);
      timer = setTimeout(rescan, 120);
    }
  });

  globalThis.CTX_SLON_MARKS = { rescan: rescan };
  rescan();
})();
'@

# =====================================================================
# 4) src/ui/slondoor.js — логика двери
# =====================================================================
$slondoor = @'
/* mem-OK · src/ui/slondoor.js · «слон» (TASK-0173).
 * Дверь: статус + descript (управляющий) + история (координата·время·автор)
 * + действия сменить/скрыть/удалить (с подтверждением) + ранги (открытая структура).
 * Пишет ТОЛЬКО через сообщения в background (единственный писатель). */
(function () {
  "use strict";

  var coord = decodeURIComponent(location.hash.slice(1) || "");
  var state = { status: null, ranks: [] };

  function $(id) { return document.getElementById(id); }
  function send(type, payload) { return chrome.runtime.sendMessage({ type: type, payload: payload }); }

  function fmtTs(ts) {
    var d = new Date(ts);
    function p(n) { return String(n).padStart(2, "0"); }
    return d.getFullYear() + "-" + p(d.getMonth() + 1) + "-" + p(d.getDate()) + " " + p(d.getHours()) + ":" + p(d.getMinutes());
  }

  function badgeHtml(st) {
    var rank = null;
    for (var i = 0; i < state.ranks.length; i++) if (state.ranks[i].id === st.rankId) rank = state.ranks[i];
    var name = rank ? rank.rank : (st.rankId == null ? "без ранга" : "ранг #" + st.rankId);
    var bgcolor = rank && rank.bgcolor ? rank.bgcolor : "#8a94a3";
    var fontcolor = rank && rank.fontcolor ? rank.fontcolor : "#000000";
    return { name: name, bgcolor: bgcolor, fontcolor: fontcolor };
  }

  function render() {
    var st = state.status;
    var cur = $("curStatus");
    var rankSel = $("rankSel");
    var descript = $("descript");
    var hist = $("hist");
    var btnHide = $("btnHide");

    /* текущий статус */
    cur.innerHTML = "";
    if (st) {
      var b = badgeHtml(st);
      var badge = document.createElement("span");
      badge.className = "badge";
      badge.style.background = b.bgcolor;
      badge.style.color = b.fontcolor;
      badge.textContent = b.name;
      cur.appendChild(badge);
      if (st.hidden) {
        var h = document.createElement("span");
        h.className = "flag";
        h.textContent = "скрыт";
        cur.appendChild(h);
      }
      descript.value = st.descript || "";
      btnHide.textContent = st.hidden ? "Показать" : "Скрыть";
    } else {
      cur.innerHTML = '<span class="none">статуса нет — выберите ранг и сохраните</span>';
      descript.value = "";
      btnHide.textContent = "Скрыть";
    }

    /* селект рангов */
    rankSel.innerHTML = '<option value="">— без ранга —</option>';
    state.ranks.forEach(function (r) {
      var o = document.createElement("option");
      o.value = String(r.id);
      o.textContent = r.rank;
      if (st && st.rankId === r.id) o.selected = true;
      rankSel.appendChild(o);
    });

    /* история: координата · время · автор · действие (новые сверху) */
    hist.innerHTML = "";
    var hs = (st && st.history) ? st.history.slice().reverse() : [];
    if (!hs.length) {
      hist.innerHTML = '<li class="empty">история пуста</li>';
    } else {
      hs.forEach(function (h) {
        var li = document.createElement("li");
        li.innerHTML = "";
        var c = document.createElement("span"); c.className = "h-coord"; c.textContent = h.coord; li.appendChild(c);
        var t = document.createElement("span"); t.className = "h-ts"; t.textContent = fmtTs(h.ts); li.appendChild(t);
        var a = document.createElement("span"); a.className = "h-author"; a.textContent = h.author; li.appendChild(a);
        var act = document.createElement("span"); act.className = "h-action"; act.textContent = h.action; li.appendChild(act);
        hist.appendChild(li);
      });
    }

    /* ранги — открытая структура */
    var rl = $("rankList");
    rl.innerHTML = "";
    if (!state.ranks.length) {
      rl.innerHTML = '<li class="empty">рангов нет — структура открытая, добавьте первый</li>';
    } else {
      state.ranks.forEach(function (r) {
        var li = document.createElement("li");
        li.className = "rank-row";
        var sw = document.createElement("span"); sw.className = "sw"; sw.style.background = r.bgcolor || "#8a94a3"; li.appendChild(sw);
        var nm = document.createElement("span"); nm.className = "nm"; nm.textContent = r.rank + (r.descript ? " — " + r.descript : ""); li.appendChild(nm);
        var del = document.createElement("button");
        del.className = "mini danger";
        del.type = "button";
        del.textContent = "×";
        del.title = "удалить ранг";
        del.addEventListener("click", function () {
          if (!confirm("Удалить ранг «" + r.rank + "»? Действие явное (No implicit data removal).")) return;
          send("SLON_DEL_RANK", { id: r.id, confirm: true }).then(load);
        });
        li.appendChild(del);
        rl.appendChild(li);
      });
    }
  }

  function load() {
    send("SLON_GET_DOOR", { coord: coord }).then(function (res) {
      state.status = res.status;
      state.ranks = res.ranks || [];
      render();
    });
  }

  $("coordLabel").textContent = coord || "—";

  $("btnSet").addEventListener("click", function () {
    var author = $("author").value.trim();
    if (!author) { alert("Автор занесения обязателен (координата + время + автор)."); return; }
    var rv = $("rankSel").value;
    send("SLON_SET_STATUS", {
      coord: coord,
      author: author,
      rankId: rv === "" ? null : Number(rv),
      descript: $("descript").value,
      hidden: state.status ? !!state.status.hidden : false,
    }).then(function (r) { if (r && r.ok) load(); else alert("Ошибка: " + ((r && r.error) || "неизвестно")); });
  });

  $("btnHide").addEventListener("click", function () {
    var author = $("author").value.trim();
    if (!author) { alert("Автор занесения обязателен."); return; }
    var nh = !(state.status && state.status.hidden);
    send("SLON_HIDE", { coord: coord, author: author, hidden: nh }).then(load);
  });

  $("btnDel").addEventListener("click", function () {
    var author = $("author").value.trim();
    if (!author) { alert("Автор занесения обязателен."); return; }
    if (!confirm("Удалить статус для «" + coord + "» полностью?\nЯвное действие: история будет удалена (No implicit data removal).")) return;
    send("SLON_DELETE", { coord: coord, author: author, confirm: true }).then(function () { window.close(); });
  });

  $("btnAddRank").addEventListener("click", function () {
    var name = $("newRankName").value.trim();
    if (!name) { alert("Имя ранга обязательно."); return; }
    send("SLON_ADD_RANK", {
      rank: name,
      descript: $("newRankDesc").value,
      bgcolor: $("newRankColor").value,
      /* fontcolor/bold/italic — по умолчанию из обработчика (эталонные) */
    }).then(function () {
      $("newRankName").value = "";
      $("newRankDesc").value = "";
      load();
    });
  });

  load();
})();
'@

# =====================================================================
# 5) ui/door.html — дверь (crafted-интерфейс, светлая тема + переключатель)
# =====================================================================
$doorhtml = @'
<!DOCTYPE html>
<html lang="ru" data-theme="light">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<title>Слон · дверь</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@600;800&family=Golos+Text:wght@400;500;600;700&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet"/>
<style>
  :root{
    --bg:#f2f1ec; --surface:#ffffff; --surface2:#f7f6f2; --ink:#23272b; --mut:#6b7280;
    --line:#e2dfd6; --line2:#cfcaba; --accent:#b4690e; --accent-soft:#f6ead8;
    --ok:#2f7d4f; --danger:#b3372c; --mono:#5c6470;
    --glow:rgba(180,105,14,.10);
  }
  [data-theme="dark"]{
    --bg:#15171a; --surface:#1d2024; --surface2:#23262b; --ink:#e8e6df; --mut:#9aa1ab;
    --line:#2c3037; --line2:#3b4048; --accent:#e0912f; --accent-soft:#33281a;
    --ok:#4fb27a; --danger:#e06a5e; --mono:#a7aeb8;
    --glow:rgba(224,145,47,.08);
  }
  *{box-sizing:border-box}
  body{
    margin:0; background:var(--bg); color:var(--ink);
    font:15px/1.55 "Golos Text","Segoe UI",sans-serif;
    transition:background .3s,color .3s; -webkit-font-smoothing:antialiased;
  }
  body::before{
    content:""; position:fixed; inset:0; pointer-events:none; z-index:0;
    background:radial-gradient(560px 340px at 88% -8%, var(--glow), transparent 70%);
  }
  .wrap{position:relative; z-index:1; max-width:440px; margin:0 auto; padding:22px 20px 30px}

  .mast{display:flex; align-items:center; gap:12px; margin-bottom:18px; animation:drop .45s cubic-bezier(.22,.61,.36,1)}
  .ele{width:44px; height:44px; flex:none; border-radius:12px; background:var(--accent-soft);
    display:flex; align-items:center; justify-content:center; transition:transform .2s}
  .ele:hover{transform:rotate(-6deg) scale(1.05)}
  .ele svg{width:26px; height:26px; stroke:var(--accent)}
  .mast h1{margin:0; font:800 20px/1.1 "Unbounded",sans-serif; letter-spacing:.01em}
  .coord{font:600 12px "JetBrains Mono",monospace; color:var(--accent); word-break:break-all}
  .tbtn{margin-left:auto; flex:none; border:1px solid var(--line2); background:var(--surface);
    color:var(--ink); border-radius:999px; width:34px; height:34px; cursor:pointer; font-size:15px;
    transition:transform .15s,border-color .15s}
  .tbtn:hover{transform:translateY(-2px); border-color:var(--accent)}

  .card{background:var(--surface); border:1px solid var(--line); border-radius:12px;
    padding:16px 16px 14px; margin-bottom:14px; box-shadow:0 1px 2px rgba(0,0,0,.04);
    animation:rise .5s cubic-bezier(.22,.61,.36,1) backwards; transition:border-color .2s}
  .card:hover{border-color:var(--line2)}
  .card:nth-of-type(1){animation-delay:.05s}
  .card:nth-of-type(2){animation-delay:.12s}
  .card:nth-of-type(3){animation-delay:.19s}
  .card h2{margin:0 0 12px; font:600 11px "JetBrains Mono",monospace; letter-spacing:.14em;
    text-transform:uppercase; color:var(--mut)}

  .badge{display:inline-block; padding:3px 12px; border-radius:999px; color:#fff;
    font:600 12px "Golos Text",sans-serif; animation:pulse 2.4s ease-in-out infinite}
  .flag{display:inline-block; margin-left:8px; padding:2px 8px; border-radius:999px;
    border:1px solid var(--line2); color:var(--mut); font:600 10px "JetBrains Mono",monospace}
  .none{color:var(--mut); font-size:13px; font-style:italic}

  label{display:block; margin-top:12px; font:600 11px "JetBrains Mono",monospace;
    letter-spacing:.08em; text-transform:uppercase; color:var(--mut)}
  select,textarea,input[type="text"]{
    width:100%; margin-top:5px; padding:8px 10px; font:14px "Golos Text",sans-serif;
    color:var(--ink); background:var(--surface2); border:1px solid var(--line);
    border-radius:8px; outline:none; transition:border-color .15s,box-shadow .15s}
  select:focus,textarea:focus,input[type="text"]:focus{border-color:var(--accent);
    box-shadow:0 0 0 3px var(--glow)}
  textarea{resize:vertical; min-height:52px}

  .row{display:flex; gap:8px; margin-top:14px}
  button{cursor:pointer; font:600 13px "Golos Text",sans-serif; border-radius:8px;
    padding:9px 0; border:1px solid transparent; transition:transform .12s,filter .12s}
  button:hover{transform:translateY(-1px)}
  button:active{transform:translateY(0)}
  .primary{flex:1; background:var(--accent); color:#fff}
  .primary:hover{filter:brightness(1.08)}
  .ghost{flex:0 0 auto; padding:9px 14px; background:var(--surface2); border-color:var(--line); color:var(--ink)}
  .danger{background:transparent; border-color:var(--danger); color:var(--danger); padding:9px 14px}
  .danger:hover{background:var(--danger); color:#fff}

  #hist{list-style:none; margin:0; padding:0}
  #hist li{display:grid; grid-template-columns:1fr auto; gap:2px 10px; padding:8px 2px;
    border-bottom:1px dashed var(--line); font-size:12.5px; transition:background .15s}
  #hist li:last-child{border-bottom:none}
  #hist li:hover{background:var(--surface2)}
  .h-coord{font:600 12px "JetBrains Mono",monospace; color:var(--ink); word-break:break-all}
  .h-ts{font:400 11px "JetBrains Mono",monospace; color:var(--mut); text-align:right}
  .h-author{font:600 12px "Golos Text",sans-serif; color:var(--accent)}
  .h-action{font:400 11px "JetBrains Mono",monospace; color:var(--mut); text-align:right}

  #rankList{list-style:none; margin:0 0 12px; padding:0}
  .rank-row{display:flex; align-items:center; gap:9px; padding:7px 2px; border-bottom:1px dashed var(--line)}
  .rank-row:last-child{border-bottom:none}
  .sw{width:14px; height:14px; flex:none; border-radius:4px}
  .nm{flex:1; font-size:13px; word-break:break-word}
  .mini{flex:none; width:24px; height:24px; padding:0; border-radius:6px; border:1px solid var(--line);
    background:transparent; color:var(--mut); font-size:14px; line-height:1}
  .mini.danger{border-color:transparent; color:var(--danger)}
  .mini.danger:hover{background:var(--danger); color:#fff}
  .add{display:grid; grid-template-columns:1fr 1fr auto auto; gap:8px; align-items:end}
  .add input[type="color"]{width:38px; height:34px; padding:2px; border:1px solid var(--line);
    border-radius:8px; background:var(--surface2); cursor:pointer}
  .add button{padding:8px 12px; background:var(--surface2); border-color:var(--line); color:var(--ink)}
  .empty{color:var(--mut); font-style:italic; font-size:13px; padding:6px 2px}

  @keyframes rise{from{opacity:0; transform:translateY(14px)} to{opacity:1; transform:none}}
  @keyframes drop{from{opacity:0; transform:translateY(-10px)} to{opacity:1; transform:none}}
  @keyframes pulse{0%,100%{box-shadow:0 0 0 0 var(--glow)} 50%{box-shadow:0 0 0 6px transparent}}
  @media (prefers-reduced-motion: reduce){
    *{animation:none!important; transition:none!important}
  }
</style>
</head>
<body>
<div class="wrap">

  <div class="mast">
    <span class="ele" title="слон">
      <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M7 20v-4a5 5 0 0 1-2-9 6 6 0 0 1 11-2 5 5 0 0 1 3 9v6"/>
        <path d="M12 20v-5"/>
        <path d="M19 10c1.5 0 2.5 1 2.5 2.5S20 15 19 15"/>
      </svg>
    </span>
    <div>
      <h1>Слон · дверь</h1>
      <div class="coord" id="coordLabel">—</div>
    </div>
    <button class="tbtn" id="themeToggle" type="button" title="тема">◐</button>
  </div>

  <section class="card">
    <h2>Статус</h2>
    <div id="curStatus"></div>
    <label for="rankSel">Ранг</label>
    <select id="rankSel"></select>
    <label for="descript">Пояснение · descript (пишет управляющий)</label>
    <textarea id="descript" placeholder="что держать в голове про эту координату"></textarea>
    <label for="author">Автор занесения · обязательно</label>
    <input id="author" type="text" placeholder="кто вносит запись"/>
    <div class="row">
      <button id="btnSet" class="primary" type="button">Сохранить статус</button>
      <button id="btnHide" class="ghost" type="button">Скрыть</button>
      <button id="btnDel" class="danger" type="button">Удалить</button>
    </div>
  </section>

  <section class="card">
    <h2>История · координата · время · автор</h2>
    <ul id="hist"></ul>
  </section>

  <section class="card">
    <h2>Ранги · открытая структура</h2>
    <ul id="rankList"></ul>
    <div class="add">
      <input id="newRankName" type="text" placeholder="имя ранга"/>
      <input id="newRankDesc" type="text" placeholder="пояснение"/>
      <input id="newRankColor" type="color" value="#b4690e" title="цвет"/>
      <button id="btnAddRank" type="button">+</button>
    </div>
  </section>

</div>
<script>
  (function(){
    function apply(t){
      document.documentElement.setAttribute("data-theme", t);
      try{ localStorage.setItem("slon-theme", t); }catch(e){}
    }
    var saved = null;
    try{ saved = localStorage.getItem("slon-theme"); }catch(e){}
    apply(saved === "dark" ? "dark" : "light");
    document.getElementById("themeToggle").addEventListener("click", function(){
      apply(document.documentElement.getAttribute("data-theme") === "light" ? "dark" : "light");
    });
  })();
</script>
<script src="../src/ui/slondoor.js"></script>
</body>
</html>
'@

# =====================================================================
# Создаём файлы
# =====================================================================
Write-File "src/core/slonstore.js" $slonstore
Write-File "src/slonbg.js"         $slonbg
Write-File "src/ui/slonmarks.js"   $slonmarks
Write-File "src/ui/slondoor.js"    $slondoor
Write-File "ui/door.html"          $doorhtml

# =====================================================================
# Правка 1: background.js — importScripts (идемпотентно)
# =====================================================================
$bgPath = Join-Path $ext "src/background.js"
$bg = Get-Content -LiteralPath $bgPath -Raw
if ($bg -notmatch [regex]::Escape("TASK-0173 слон")) {
    $anchor = 'importScripts("./core/storage.js");'
    $insert = $anchor + "`n" + 'importScripts("./core/slonstore.js"); // TASK-0173 слон' + "`n" + 'importScripts("./slonbg.js"); // TASK-0173 слон'
    if ($bg.Contains($anchor)) {
        $bg = $bg.Replace($anchor, $insert)
        Set-Content -LiteralPath $bgPath -Value $bg -Encoding utf8 -NoNewline
        Write-Host "правлен: EdgeExtension/src/background.js (+importScripts слона)" -ForegroundColor Yellow
    } else {
        Write-Error "background.js: не найден якорь importScripts storage.js — вставьте importScripts вручную"
    }
} else {
    Write-Host "background.js: importScripts слона уже на месте" -ForegroundColor DarkGray
}

# =====================================================================
# Правка 2: manifest.json — content_scripts + версия (идемпотентно)
# =====================================================================
$mfPath = Join-Path $ext "manifest.json"
$mf = Get-Content -LiteralPath $mfPath -Raw | ConvertFrom-Json -AsHashtable
$js = $mf.content_scripts[0].js
if ($js -notcontains "src/core/slonstore.js") {
    $idx = [array]::IndexOf($js, "src/core/storage.js")
    if ($idx -ge 0) { $js = $js[0..$idx] + @("src/core/slonstore.js") + $js[($idx+1)..($js.Length-1)] }
    else { $js = @($js) + @("src/core/slonstore.js") }
}
if ($js -notcontains "src/ui/slonmarks.js") {
    $idx2 = [array]::IndexOf($js, "src/ui/layer.js")
    if ($idx2 -ge 0) { $js = $js[0..$idx2] + @("src/ui/slonmarks.js") + $js[($idx2+1)..($js.Length-1)] }
    else { $js = @($js) + @("src/ui/slonmarks.js") }
}
$mf.content_scripts[0].js = $js
$mf.version = "0.2.0"
$mf | ConvertTo-Json -Depth 20 | Set-Content -LiteralPath $mfPath -Encoding utf8
Write-Host "правлен: EdgeExtension/manifest.json (content_scripts + version 0.2.0)" -ForegroundColor Yellow

# =====================================================================
# Отчёт
# =====================================================================
Write-Host ""
Write-Host "=== СЛОН (TASK-0173) собран ===" -ForegroundColor Cyan
Write-Host "создано:" -ForegroundColor Green
Write-Host "  src/core/slonstore.js · src/slonbg.js · src/ui/slonmarks.js · src/ui/slondoor.js · ui/door.html"
Write-Host "правлено:" -ForegroundColor Yellow
Write-Host "  src/background.js (importScripts) · manifest.json (content_scripts, 0.2.0)"
Write-Host ""
Write-Host "Ядро v07g (messaging/normalize/storage/layer) и ок-адаптация НЕ тронуты." -ForegroundColor DarkGray
Write-Host "Далее: pwsh scripts/Build.ps1 -> Reload -> ok.ru -> ПКМ «Слон: статус…»" -ForegroundColor DarkGray
Write-Host "Коммит: feat: слон — метка и дверь по эталону, ranks эталонные [TASK-0173]" -ForegroundColor Cyan

