Приветствую. Сверка проведена.

### 1. Что показал коммит `900295e`
Вы создали файл `docs/answers/answers/to-TASK-0174.md`, но **он пустой (0 байт)**. Вероятно, при копировании через веб-морду текст не вставился. Но не страшно — я уже вычитал полный текст ответа Кодера с его блога по ссылке, которую вы дали.

### 2. Суть ответа Кодера (диагноз)
Кодер пишет, что **не может выполнить моё ТЗ по апгрейду UX, потому что у него в воркспейсе физически нет самих файлов слона** (`slondoor.js`, `door.html`, `slonmarks.js`, `slonbg.js`). 
В папке `/workspace/EdgeExtension/src/ui/` у него лежат только старые файлы (`dialog.js` и `layer.js`). Скрипт сборки, который должен был их создать по `TASK-0173-01.md`, видимо, не отработал в его песочнице. 
Кодер задаёт логичный вопрос: *"Создать недостающие файлы слона согласно эталону из TASK-0173-01.md? После создания — внести требуемые правки по автозахвату и чистоте полей?"*

### 3. Регламент оставления ответов для МЕНЯ
Вы всё сделали абсолютно правильно, заложив файл в папку `docs/answers/answers/`. Это идеальная парковка для архива. 
**Правило на будущее:**
1. Создаёте файл `docs/answers/answers/to-TASK-NNNN.md` (NNNN — номер задачи).
2. **Обязательно вставляете текст внутрь файла** (а не просто создаёте пустой).
3. Даёте коммит (например, `docs: Ответ Кодера по TASK-NNNN`).
4. Пишете мне сюда в чат: *"Ответ Кодера в коммите [хеш]"* или *"Ответ Кодера на блоге [ссылка]"*.

---

### 4. Ваша директива Кодеру (несёте её в чат к Кодеру прямо сейчас)

Кодер ждёт отмашки. Скопируйте блок ниже целиком и отправьте ему.

---
**[БЛОК ДЛЯ ВСТАВКИ КОДЕРУ — КОПИРОВАТЬ ЦЕЛИКОМ]**

**ПОДТВЕРЖДЕНИЕ ОТ ПРОЕКТИРОВЩИКА (TASK-0174)**

1. **Создание базы:** Да, создай недостающие файлы слона (`src/core/slonstore.js`, `src/slonbg.js`, `src/ui/slonmarks.js`, `src/ui/slondoor.js`, `ui/door.html`) строго согласно эталону и логике из `docs/tasks/TASK-0173-01.md`.
2. **Интеграция ТЗ по UX:** Сразу при создании этих файлов (или сразу после) примени к ним мою предыдущую директиву:
   * **Автозахват:** `slondoor.js` должен сам парсить координату из URL вкладки (`location.href` или из `sender.tab.url` в background).
   * **Чистота полей:** В `ui/door.html` поле `descript` (Пояснение) при открытии новой записи должно быть **СТРОГО ПУСТЫМ** (`<textarea ...></textarea>` или `value=""`). Никаких placeholder с чужими текстами или мифологемами. Управляющий пишет смыслы с нуля.
   * **Бинарный выбор:** Форма должна иметь кнопки «Сохранить» и «Сброс» (отмена).
3. **Манифест:** Убедись, что `manifest.json` корректно подключает `slondoor.js` и `slonmarks.js` для хоста `*://*.ok.ru/*`.

Исполнить создание и апгрейд одним шагом. Жду отчёта и диффа (diff) созданных/изменённых файлов.

**[КОНЕЦ БЛОКА ДЛЯ КОДЕРА]**

---
**Следующий шаг:**
Как только Кодер создаст файлы и пришлёт вам диффы (или скажет, что файлы созданы в репо) — напишите мне сюда: **«Кодер создал файлы, давай проверять персистентность»**. Я дам вам алгоритм сборки и тестирования метки в браузере. 

Дверца видима, ключ у вас.