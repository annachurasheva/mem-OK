# TASK-0176 · Директива проектировщика (ревизия 2) · дефекты 2–4 + формат отдачи

Координата: 2026-09-10 · annachurasheva/mem-OK · HEAD 1b3a10c.
Проектировщик: Дельта. Исполнитель: Кодер. Управляющий: Anna-Ch
(дефект 1 и заполнение рангов берёт на себя).

## 0. ФОРМАТ ОТДАЧИ КОДА (обязательно, это главное в ревизии 2)
Копипаста с видимого экрана ЗАПРЕЩЕНА: она режет строки (доказательство:
строка ранга оборвалась на полуслове «Может сорвать»).
Отдавать только через панель Code Tool Record:
1. по каждому изменённому файлу открыть вкладку Diff;
2. по каждому файлу нажать Copy (или Download) — ПОЛНЫЙ текст файла, не фрагмент;
3. в отчёт вставить: имя файла + полный текст, полученный через Copy.
Ничего дописывать руками с экрана нельзя. Управляющий вставляет в репо
через веб-морду только из вашего Copy — чтобы в репо легло ровно то, что вы написали.

## Дефект 2 (мина А): двойной импорт slonstore
Файл: EdgeExtension/src/slonbg.js.
Удалить строку 6: importScripts('core/slonstore.js');
Причина: slonstore.js уже загружен строкой 9 background.js; повторный importScripts
повторно исполняет тот же файл → повторное объявление const/class → SyntaxError →
падают slonbg.js, а следом весь background.js (исчезнут оба меню).
Порядок гарантирован самим background.js (slonstore раньше slonbg).
Защитный импорт НЕ нужен. Больше в slonbg.js не менять ничего.

## Дефект 3 (мина Б): removeAll стирает slon-door
Файл: EdgeExtension/src/background.js, обработчик onInstalled.
Внутри колбэка chrome.contextMenus.removeAll, сразу после
chrome.contextMenus.create({ id: "zapiska-soo", ... });
добавить строку:
createSlonMenu();
Функция глобальна (приходит из slonbg.js). Порядок станет детерминированным:
zapiska-per, zapiska-soo, slon-door. Больше в background.js не менять ничего.

## Дефект 4: ранги стартуют с нуля
Файл: EdgeExtension/src/core/slonstore.js.
Массив DEFAULT_RANKS заменить на пустой:
const DEFAULT_RANKS = [];
Никаких рангов из dmiandr/context и никаких предзаполнений. Управляющий
заполняет ранги САМ через дверь и сам тестирует.
Поддержать пустоту без новой логики:
- селект ранга в двери показывает единственный пункт «— без ранга —»;
- список рангов показывает «рангов нет — структура открытая, добавьте первый»;
- сохранение статуса с rankId = null работает.
Проверить slondoor.js и door.html на терпимость к пустому массиву; если не терпят —
минимальная правка только под пустой массив, ничего сверх.

## НЕ ТРОГАТЬ
manifest.json, layer.js, content.js, slonmarks.js, ядро v07g, .vscode/, docs/.
(door.html и slondoor.js — только в объёме дефекта 4.)

## Сборка
pwsh scripts/Build.ps1 — исполняет управляющий на своей стороне, вы не гоняете.

## Отчёт Кодера (строго по формату п.0)
1. Список файлов: slonbg.js, background.js, slonstore.js
   (+ slondoor.js / door.html, если потребовалось по дефекту 4).
2. По каждому файлу — ПОЛНЫЙ текст через Copy из вкладки Diff.
3. Одна строка в конце: «меню: третий пункт готов к проверке».

Commit message (для управляющего):
fix: слон — дефекты 2–4 (двойной импорт, removeAll, ранги-зеро) [TASK-0176]
